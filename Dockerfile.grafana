# Dockerfile.grafana
FROM grafana/grafana:latest

# Copy provisioning config (datasources, dashboards)
COPY grafana/provisioning/ /etc/grafana/provisioning/

# No chown needed — files are readable by UID 472
USER 472

EXPOSE 3000